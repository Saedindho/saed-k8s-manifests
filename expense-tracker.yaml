
metadata:
  name: expense-tracker-deployment
spec:
  selector:
    matchLabels:
      app: expense-tracker  # This MUST match the template label below
  template:
    metadata:
      labels:
        app: expense-tracker # And this MUST match the Service selector
